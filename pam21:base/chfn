#
# The PAM configuration file for the Shadow `chfn' service

auth required pam_unix.so 
account sufficient  pam_time.so 
account required pam_deny.so

# auth required pam_unix.so try_firts_pass (prueba la passwd anterior antes de mostrar nada) use_firts_pass (usa obligada passwd anterior, si no es valido rechaza directamente) 
